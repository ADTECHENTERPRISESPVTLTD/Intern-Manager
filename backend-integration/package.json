{
  "name": "backend-integration",
  "version": "0.1.0",
  "private": true,
  "description": "Drop-in TypeScript client that lets the Node/Express backend call the face-verification service. No Python knowledge needed.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}

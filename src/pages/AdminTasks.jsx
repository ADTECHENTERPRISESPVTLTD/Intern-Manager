import { Plus, Pencil, Archive } from "lucide-react";
import { useState } from "react";
import Card from "../components/Card";
import PageHeader from "../components/PageHeader";
const seed=[["TASK-01","DevOps, Deployment & Release Management","Akanksha Hajare","High","In Progress"],["TASK-02","Vercel & Render Setup","Akanksha Hajare","Medium","Submitted"],["TASK-04","Official Website Backend Module Development","Adarsh Gangshettiwar","High","In Progress"]];
export default function AdminTasks(){const [tasks,setTasks]=useState(seed); const [open,setOpen]=useState(false); const [title,setTitle]=useState(""); const add=()=>{if(!title.trim())return;setTasks([...tasks,[`TASK-${String(tasks.length+1).padStart(2,"0")}`,title,"Unassigned","Medium","Not Started"]]);setTitle("");setOpen(false)};return <><PageHeader eyebrow="Administration" title="Task Management" description="Create, edit, assign and archive intern tasks." action={<button className="btn btn-primary" onClick={()=>setOpen(true)}><Plus size={16}/> Create Task</button>}/><Card><div className="table-wrap"><table><thead><tr><th>ID</th><th>Title</th><th>Assigned intern</th><th>Priority</th><th>Status</th><th>Actions</th></tr></thead><tbody>{tasks.map(t=><tr key={t[0]}><td>{t[0]}</td><td><strong>{t[1]}</strong></td><td>{t[2]}</td><td>{t[3]}</td><td>{t[4]}</td><td><div className="button-row"><button className="icon-btn"><Pencil size={15}/></button><button className="icon-btn danger"><Archive size={15}/></button></div></td></tr>)}</tbody></table></div></Card>{open&&<div className="modal-backdrop"><div className="modal"><div className="eyebrow">Task management</div><h2>Create Task</h2><label>Task title<input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Task title"/></label><div className="modal-actions"><button className="btn btn-ghost" onClick={()=>setOpen(false)}>Cancel</button><button className="btn btn-primary" onClick={add}>Create Task</button></div></div></div>}</>};